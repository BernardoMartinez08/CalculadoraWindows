import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora-windows',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
